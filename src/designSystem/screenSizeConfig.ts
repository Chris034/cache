const deviceSize = {
    mobile: '425px',
    desktop: '768px',
  };

export const device = {
    mobile: `(min-width: ${deviceSize.mobile})`,
    desktop: `(min-width: ${deviceSize.desktop})`,
  };

